/* ============================================================================
 * user/main.c — Programa de usuário "v1" (ficou de lado!)
 * ----------------------------------------------------------------------------
 * Este arquivo foi o primeiro rascunho do programa em Ring 3: um loop vazio.
 * Ele foi substituído por `program.c` (que testa as syscalls) e, lá na frente,
 * pelo shell completo (`shell.c`), que é o que de fato roda hoje.
 * Mantemos o arquivo aqui só de recordação. 😄
 * ============================================================================ */

int main(void) {
    // Por enquanto, apenas um loop infinito ou código de usuário básico
    while (1) {
        // Futuramente faremos system calls aqui
    }
    return 0;
}